<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donor Request</title>
    <style>
        body {
    background-color: white;
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

h3 {
    background-color: red;
    color: white;
    text-align: center;
    padding: 10px;
}

form {
    max-width: 600px;
    margin: 20px auto;
    padding: 20px;
    background-color: #f9f9f9;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

form label {
    display: block;
    margin-top: 15px;
    font-weight: bold;
    color: #333;
}

form input[type="text"],
form input[type="number"],
form select {
    width: 100%;
    height: 40px;
    padding: 8px;
    margin-top: 8px;
    font-size: 16px;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
}

form select {
    cursor: pointer;
}

form button[type="submit"] {
    background-color: red;
    color: white;
    border: none;
    padding: 12px 20px;
    font-size: 16px;
    cursor: pointer;
    border-radius: 4px;
}

form button[type="submit"]:hover {
    background-color: darkred;
}

    </style>
</head>

<body>
    <h3>Book Your Appointment</h3>


    <hr>
    <form action="/donorRequest" method="post">
        <label for="Donor_Name">Donor Name</label>
        <input type="text" name="donorname" value="<%= donorID.name %>" required readonly>

        <label for="selectbloodgroup">Select Blood Group</label>
        <input type="text" name="donorBloodGroup" value="<%= donorID.bloodgroup %>" required readonly>
        
        <label for="Timeslot">Choose Time</label>
        <select name="Timeslot" id="slot" required>
            <% slotsTiming.forEach((slots)=>{ %>
                <option value="<%=slots.availableslots%>"><%= slots.availableslots %></option>
        <% }) %>
        </select>

        <input type="number" name="donorid" value="<%= donorID.id %>" required readonly hidden>

        </select>

        <button type="submit">Submit</button>
    </form>
</body>
</html>
